ssh-keygen -t ed25519 -C "email_github@example.com"

ssh-keygen -t rsa -b 4096 -C "email_github@example.com"

eval "$(ssh-agent -s)"

ssh-add ~/.ssh/id_ed25519

salin public key ke repository

ssh -T git@github.com

#remote jika repository sudah ada di lokal

git remote -v

git remote set-url origin git@github.com:username/nama-repo.git

git pull origin main
